{% extends 'base.html.twig' %}

{% block title %}Chantier{% endblock %}

{% block body %}
    <h1>Chantier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chantier.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ chantier.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chantier_index') }}">retour à la liste des chantiers</a>

    <a href="{{ path('chantier_edit', {'id': chantier.id}) }}">modifier</a>

    {{ include('chantier/_delete_form.html.twig') }}
{% endblock %}
